# Traceroute Frontend

Estrutura inicial do frontend em React + Tailwind.

## Scripts

```bash
npm install
npm run dev
```

## Variáveis de ambiente

Crie um `.env` no diretório `frontend`:

```bash
VITE_API_BASE_URL=http://localhost:3000
```
